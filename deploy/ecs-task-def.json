{
  "_comment": "ECS Fargate task definition template for an arc agent.",
  "_usage": [
    "Replace all <PLACEHOLDER> values before registering.",
    "Register: aws ecs register-task-definition --cli-input-json file://ecs-task-def.json",
    "Tip: set ARC_ENV=production only after agents reach Govern/Scale stage."
  ],

  "family": "<AGENT_ID>-arc",
  "taskRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/arc-agent-task-role",
  "executionRoleArn": "arn:aws:iam::<ACCOUNT_ID>:role/arc-agent-execution-role",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",

  "containerDefinitions": [
    {
      "name": "<AGENT_ID>",
      "image": "<ECR_REPO_URI>/<AGENT_ID>:latest",
      "essential": true,

      "environment": [
        { "name": "ARC_ENV",           "value": "sandbox" },
        { "name": "ARC_MANIFEST_PATH", "value": "/app/manifest.yaml" },
        { "name": "ARC_POLICY_DIR",    "value": "/app/policies/" },
        { "name": "ARC_LOG_LEVEL",     "value": "INFO" },
        { "name": "AGENT_MODULE",      "value": "<PYTHON_MODULE>" },
        { "name": "AGENT_CLASS",       "value": "<AGENT_CLASS>" }
      ],

      "secrets": [
        {
          "_comment": "Store sensitive config in SSM Parameter Store or Secrets Manager",
          "name":      "SOME_API_KEY",
          "valueFrom": "arn:aws:ssm:<REGION>:<ACCOUNT_ID>:parameter/arc/<AGENT_ID>/api-key"
        }
      ],

      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group":         "/arc/agents/<AGENT_ID>",
          "awslogs-region":        "<REGION>",
          "awslogs-stream-prefix": "agent"
        }
      },

      "healthCheck": {
        "command":     ["CMD-SHELL", "arc agent validate /app/manifest.yaml || exit 1"],
        "interval":    30,
        "timeout":     10,
        "retries":     3,
        "startPeriod": 15
      },

      "readonlyRootFilesystem": true,
      "user": "1001:1001"
    }
  ],

  "tags": [
    { "key": "arc:agent-id",        "value": "<AGENT_ID>" },
    { "key": "arc:lifecycle-stage", "value": "sandbox" },
    { "key": "arc:owner",           "value": "<TEAM_NAME>" }
  ]
}
