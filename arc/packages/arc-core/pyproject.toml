[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arc-core"
version = "0.1.0"
description = "Arc governance engine — typed effects, policy evaluation, ControlTower, audit trail"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "tollgate>=1.4",
    # .env loader for local + sandbox dev. ~50KB pure-Python, no transitive
    # deps. Production deploys (Lambda / ECS) get env from the platform's
    # task config layer; the loader is a no-op when no .env file is present.
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "ruff", "mypy"]

# Async HTTP gateway (arc.core.gateway.HttpGateway)
http = ["httpx>=0.27"]

# AWS-backed memory store (DynamoDBMemoryBackend) and other boto3-using primitives
aws = ["boto3>=1.34", "botocore>=1.34"]

[tool.hatch.build.targets.wheel]
packages = ["src/arc"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
