[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arc-orchestrators"
version = "0.1.0"
description = "Arc orchestrator adapters — swap between LangGraph, AgentCore, and Strands without changing agent code"
requires-python = ">=3.11"
dependencies = [
    "arc-core>=0.1.0",
]

[project.optional-dependencies]
# LangChain bridge (FoundryTool, FoundryToolkit, FoundryRunnable)
langchain   = ["langchain-core>=0.2", "langchain>=0.2", "langchain-community>=0.2"]
langgraph   = ["langgraph>=0.2", "langchain-aws>=0.1", "arc-orchestrators[langchain]"]
agentcore   = ["boto3>=1.34", "botocore>=1.34"]
strands     = ["strands-agents>=0.1"]
all         = ["arc-orchestrators[langchain,langgraph,agentcore,strands]"]

[tool.hatch.build.targets.wheel]
packages = ["src/arc"]
