[tool.uv.workspace]
members = [
    "packages/arc-core",
    "packages/arc-harness",
    "packages/arc-orchestrators",
    "packages/arc-connectors",
    "packages/arc-runtime",
    # arc-platform is reserved for Phase 3 (web portals); add to workspace
    # once it has a pyproject.toml and source.
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
